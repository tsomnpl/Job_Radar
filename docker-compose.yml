services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: jobradar
      POSTGRES_PASSWORD: jobradar
      POSTGRES_DB: jobradar
    ports:
      - "5432:5432"
    volumes:
      - jobradar-pg:/var/lib/postgresql/data

volumes:
  jobradar-pg:
